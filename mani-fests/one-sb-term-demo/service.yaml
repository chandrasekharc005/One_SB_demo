apiVersion: v1
kind: Service
metadata:
  name: one-sb-term-demo-svc
  namespace: insurance-demo
spec:
  selector:
    app: one-sb-term-demo
  ports:
    - port: 80
      targetPort: 80
  type: NodePort
